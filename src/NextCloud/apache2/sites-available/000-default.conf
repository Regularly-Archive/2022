<VirtualHost *:80>
	# The ServerName directive sets the request scheme, hostname and port that
	# the server uses to identify itself. This is used when creating
	# redirection URLs. In the context of virtual hosts, the ServerName
	# specifies what hostname must appear in the request's Host: header to
	# match this virtual host. For the default virtual host (this file) this
	# value is not decisive as it is used as a last resort host regardless.
	# However, you must set it for any further virtual host explicitly.
	ServerName nextcloud.fgms.dev.com

	ServerAdmin webmaster@localhost
	DocumentRoot /var/www/html

	# Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
	# error, crit, alert, emerg.
	# It is also possible to configure the loglevel for particular
	# modules, e.g.
	LogLevel debug

	ErrorLog ${APACHE_LOG_DIR}/error.log
	CustomLog ${APACHE_LOG_DIR}/access.log combined

	# SSLEngine Off
    # SSLCipherSuite HIGH:MEDIUM:!aNULL:!MD5:!SEED:!IDEA

    # SSLCertificateFile /etc/certs/nextcloud.crt
    # SSLCertificateKeyFile /etc/certs/nextcloud.key

	# For most configuration files from conf-available/, which are
	# enabled or disabled at a global level, it is possible to
	# include a line for only one particular virtual host. For example the
	# following line enables the CGI configuration for this host only
	# after it has been globally disabled with "a2disconf".
	#Include conf-available/serve-cgi-bin.conf
    BrowserMatch Windows gssapi-no-negotiate
	<Location "/">
		# AuthType Basic
		# AuthName "LDAP SSO"
		# AuthBasicProvider ldap
		# AuthLDAPBindDN "CN=administrator,OU=OPS,OU=4GMS-Users,DC=fgms,DC=dev,DC=com"
    	# AuthLDAPBindPassword Stelect2013
		# AuthLDAPURL ldap://192.168.6.30:389/OU=OPS,OU=4GMS-Users,DC=fgms,DC=dev,DC=com?sAMAccountName?one
		# Require valid-user

		# # Set the REMOTE_USER header to the contents of the LDAP query response's "uid" attribute
		# RequestHeader set REMOTE_USER %{AUTHENTICATE_uid}e
		# RequestHeader set AUTH_TYPE Basic
		# AuthType GSSAPI
    	# AuthName "GSSAPI Single Sign On Login"
    	# GssapiCredStore keytab:/etc/apache2/krb-container.keytab
    	# Require valid-user
		# GssapiLocalName On
		# GssapiAcceptorName HTTP
		AuthType GSSAPI
  		GssapiCredStore keytab:/etc/apache2/krb-container.keytab
  		GssapiAllowedMech krb5
  		# Require valid-user
		RequestHeader set REMOTE_USER %{REMOTE_USER}s
		RequestHeader set AUTH_TYPE %{AUTH_TYPE}s
    	RequestHeader set KRB5CCNAME %{KRB5CCNAME}s
		GssapiLocalName On
		GssapiAcceptorName HTTP@nextcloud.fgms.dev.com
		GssapiBasicTicketTimeout 86400
		GssapiImpersonate On
		# GssapiBasicAuth On
		GssapiPublishMech On
		# <IfModule mod_headers.c>
      	# 	Header always set Strict-Transport-Security "max-age=15552000; includeSubDomains"
    	# </IfModule>
	</Location>
</VirtualHost>

# vim: syntax=apache ts=4 sw=4 sts=4 sr noet
